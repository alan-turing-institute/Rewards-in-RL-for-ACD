[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "icml2026_code"
version = "0.0.1"
description = "Experiments for ICML 2026 paper"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["icml2026_codebase"]